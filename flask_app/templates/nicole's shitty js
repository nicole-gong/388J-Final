    <!-- <form id='route' method="POST" action="{{ url_for('trips.create_itin') }}">
                    <input type="hidden" id="checkedDataBoxes" name="checked" value="">
                    <input type="submit" value="Generate Route">
                </form> -->
    <!-- <script>
        var elements = querySelectorAll('[name="checked_box"]');
        elements.forEach(function (element) {
            alert(element.value);
            // document.addEventListener('click', function (event) {
            //     alert('Value changed:', event.target.id);
            // });
        });
    </script>
    <script>
        document.getElementById('route').addEventListener('submit', function (event) {
            var checkedElements = document.querySelectorAll('[name="checked_box"]');
            var checkedData = Array.from(checkedElements, function (element) {
                return element.id;
            });
            alert(checkedElements.length)
            document.getElementById('checkedDataBoxes').value = JSON.stringify(checkedData);
            this.submit();
        });
    </script> -->